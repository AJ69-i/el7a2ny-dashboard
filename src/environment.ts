export const firebaseConfig = {
  apiKey: "AIzaSyDZ3d8csJYQNKulQDe47skh58JgMElUEEs",
  authDomain: "el7a2ny-b70c7.firebaseapp.com",
  databaseURL: "https://el7a2ny-b70c7-default-rtdb.firebaseio.com",
  projectId: "el7a2ny-b70c7",
  storageBucket: "el7a2ny-b70c7.firebasestorage.app",
  messagingSenderId: "560346665774",
  appId: "1:560346665774:web:c8734aa10e7f3d8c4ee0bf",
  measurementId: "G-93XKX473DK"
};

/**
 * Optional reCAPTCHA v3 site key for Firebase App Check.
 * Leave empty to keep App Check disabled; set it to enable (see SECURITY.md).
 */
export const appCheckSiteKey: string = '';

/**
 * Web Push (FCM) VAPID public key — Firebase console → Project settings →
 * Cloud Messaging → Web configuration → "Web Push certificates" → generate key pair.
 * Leave empty to keep background push disabled (in-app realtime alerts still work).
 */
export const fcmVapidKey: string = '';
